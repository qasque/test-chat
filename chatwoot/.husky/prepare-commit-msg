#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Some environments append an IDE footer line to the message; strip before commit.
msg="$1"
[ -n "$msg" ] && [ -f "$msg" ] || exit 0
perl -i -ne 'print unless /^\s*made-with:\s*cursor\s*$/i' "$msg"
